<?php

declare(strict_types=1);

return [
    'base_url' => 'https://v.sitename.org',
    'db_path' => __DIR__ . '/var/media_watch.sqlite',
    'media_roots' => [
        '/mnt/storage/Media/Video/Movie',
        '/mnt/storage/Media/Video/Series',
    ],
    'site_name' => 'Media Watch',

    // Bearer token for the registration HTTP API (POST /api/register,
    // DELETE /api/records/{id}). Generate a long random string, e.g.
    //   php -r "echo bin2hex(random_bytes(32)), \"\n\";"
    // Leave empty to disable the API entirely (returns 503).
    'api_token' => '',

    // Hand file streaming to Apache via X-Sendfile so downloads carry
    // a proper Content-Length (browsers show ETA / remaining bytes).
    // Requires libapache2-mod-xsendfile + an XSendFilePath whitelist;
    // see env.md for the full setup.
    'use_xsendfile' => false,
];

